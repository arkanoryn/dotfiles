# -*- mode: snippet -*-
# name: reducer
# key: reducer
# --
import { $1 } from './$2';

const initialState = {
  $3
};

const $4 = (state = initialState, action) => {
   switch (action.type) {
    case $5:
      return ({
        ...state,
        $0,
      });

      default:
        return state;
  }
};

export default $4;
