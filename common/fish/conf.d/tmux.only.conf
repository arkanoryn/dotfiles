# Tmux configuration for fish plugin (non-fixterm mode)
# This file is passed to tmux with -f when fish_tmux_fixterm is false
# Only valid tmux commands are allowed here - no shell syntax

# Set terminal type
set -g default-terminal tmux-256color
